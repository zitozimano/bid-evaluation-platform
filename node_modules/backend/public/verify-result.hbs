<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">
<head>
  <meta charset="UTF-8" />
  <title>Evaluation Verification Result</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <!-- Bootstrap CSS (CDN) -->
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    rel="stylesheet"
  />

  <style>
    body {
      font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
      padding-top: 30px;
      padding-bottom: 30px;
    }
    .hash-box {
      font-family: monospace;
      font-size: 0.8rem;
      word-break: break-all;
      background: rgba(0, 0, 0, 0.2);
      padding: 0.75rem;
      border-radius: 0.25rem;
      border: 1px dashed rgba(255, 255, 255, 0.2);
    }
    .status-icon {
      font-size: 2rem;
      margin-right: 0.5rem;
    }
    .card {
      border-radius: 0.75rem;
    }
    .back-link {
      text-decoration: none;
    }
  </style>
</head>

<body class="bg-dark text-light">
  <div class="container">
    <div class="row justify-content-center">
      <div class="col-12 col-md-8 col-lg-6">

        <div class="mb-4 text-center">
          <h1 class="h3 mb-1">Evaluation Verification</h1>
          <p class="text-secondary mb-0">
            Bid Evaluation Platform • Public Verification
          </p>
        </div>

        <div class="card bg-secondary-subtle text-light shadow-sm">
          <div class="card-body">

            {{#if result.valid}}
              <div class="d-flex align-items-center mb-3">
                <span class="status-icon text-success">✔</span>
                <div>
                  <h2 class="h5 mb-0 text-success">Valid Evaluation Pack</h2>
                  <small class="text-success-emphasis">
                    This pack matches the original sealed record.
                  </small>
                </div>
              </div>
            {{else}}
              <div class="d-flex align-items-center mb-3">
                <span class="status-icon text-danger">✖</span>
                <div>
                  <h2 class="h5 mb-0 text-danger">Invalid or Unknown Hash</h2>
                  <small class="text-danger-emphasis">
                    No matching evaluation pack was found for this hash.
                  </small>
                </div>
              </div>
            {{/if}}

            <div class="mb-3">
              <label class="form-label fw-semibold">Verification Hash</label>
              <div class="hash-box">
                {{result.hash}}
              </div>
            </div>

            {{#if result.valid}}
              <div class="row mb-2">
                <div class="col-5 text-secondary small">Tender ID</div>
                <div class="col-7 small">{{result.tenderId}}</div>
              </div>
              <div class="row mb-2">
                <div class="col-5 text-secondary small">Run Number</div>
                <div class="col-7 small">{{result.runNumber}}</div>
              </div>
              <div class="row mb-2">
                <div class="col-5 text-secondary small">Timestamp</div>
                <div class="col-7 small">{{result.timestamp}}</div>
              </div>
              <div class="row mb-2">
                <div class="col-5 text-secondary small">Status</div>
                <div class="col-7 small text-success fw-semibold">Valid</div>
              </div>
            {{else}}
              <div class="row mb-2">
                <div class="col-5 text-secondary small">Status</div>
                <div class="col-7 small text-danger fw-semibold">Invalid</div>
              </div>
            {{/if}}

          </div>
        </div>

        <div class="mt-3 text-center">
          <a href="/verify" class="back-link text-info">
            ← Verify another hash
          </a>
        </div>

      </div>
    </div>
  </div>

  <!-- Bootstrap JS (optional) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
