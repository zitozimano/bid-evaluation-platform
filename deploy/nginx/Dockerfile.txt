FROM nginx:1.27-alpine

# Copy Nginx config
COPY nginx.conf /etc/nginx/nginx.conf

# Copy built frontend (run `npm run build` in frontend first)
COPY ./dist /usr/share/nginx/html

EXPOSE 80 443

CMD ["nginx", "-g", "daemon off;"]
